function myFunction():void{

    //some code

    if (someVariable != null){
        trace(someVariable);
    } else {
        trace("someVariable is null");
        // Handle the null case appropriately (e.g., assign a default value or log an error)
    }
}